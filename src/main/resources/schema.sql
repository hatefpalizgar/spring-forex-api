CREATE TABLE candles
(
    datetime VARCHAR(100), -- It is much more simpler to get dateTime as string and then convert it for further usage
    open     double,
    high     double,
    low      double,
    close    double
);